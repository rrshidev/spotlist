import { NewSessionPageClient } from './NewSessionPageClient';

export const metadata = {
  title: 'Создать сессию - SpotList',
  description: 'Создайте встречу или джем на споте',
};

export default function NewSessionPage() {
  return <NewSessionPageClient />;
}
